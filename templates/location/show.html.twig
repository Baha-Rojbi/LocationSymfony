{% extends 'base.html.twig' %}

{% block title %}Location{% endblock %}

{% block body %}
    <h1>Location</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>IdLocation</th>
            <td>{{ location.idLocation }}</td>
        </tr>
        <tr>
            <th>DateDebut</th>
            <td>{{ location.dateDebut ? location.dateDebut|date('Y-m-d') : '' }}</td>
        </tr>
        <tr>
            <th>DateFin</th>
            <td>{{ location.dateFin ? location.dateFin|date('Y-m-d') : '' }}</td>
        </tr>
        <tr>
            <th>PrixLocation</th>
            <td>{{ location.prixLocation }}</td>
        </tr>
        <tr>
            <th>IdVoiture</th>
            <td>{{ location.idVoiture }}</td>
        </tr>
        <tr>
            <th>Matricule</th>
            <td>{{ location.matricule }}</td>
        </tr>
        <tr>
            <th>Modele</th>
            <td>{{ location.modele }}</td>
        </tr>
        <tr>
            <th>ImageVoiture</th>
            <td>{{ location.imageVoiture }}</td>
        </tr>
        <tr>
            <th>IdClient</th>
            <td>{{ location.idClient }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_location_index') }}">back to list</a>

    <a href="{{ path('app_location_edit', {'idLocation': location.idLocation}) }}">edit</a>

    {{ include('location/_delete_form.html.twig') }}
{% endblock %}
